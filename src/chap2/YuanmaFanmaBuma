//原码：十进制数据的二进制表现，最左边是符号位，0为正，1为负
    //反码：正数的补码反码是其本身，负数的反码是符号位保持不变，其余为取反
    //补码：正数的补码是其本身，负数的补码是在其反码的基础上+1
    //56——原码：00111000,原码中一个0/1，称为一个bite，8个bite为一个字节，字节是最小的存储单元
    //一个字节最大值为：01111111，最小值为：11111111
    //若用原码计算负数，则结果会与预期相反，为了解决原码不能计算负数的问题，发明了反码
    //正数的反码不变，负数的反码在原码的基础上，符号位不变。数值取反，0变1，1变0
    //负数运算，先取反码，取完反码后用反码相加，然后再将反码变回原码即可
    //反码有一个弊端：10000000与00000000都表示0，所以产生了补码，负数的补码是反码+1，正数的补码是其本身
    //所以负数直接用补码加即可，所以计算机中数字的存储和计算都是用补码操作的
    //所以一个byte范围是-128到127
    //0为false，1为true
    //& 只有都为1时取1
    //|有一个为1就取1
    //<<左移，低位补0（左移一次二进制，十进制对应为数字*2）
    //>>右移，原来为负数，则高位补1；原来为正数，则高位补0；
